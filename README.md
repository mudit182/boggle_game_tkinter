# Boggle Board Generator And Words Finder

## Setup
Requirement
- Python 3.11

Install Dependencies
- Create python virtual environment
- cd into project
- Run `pip install -r requirements.txt`

Launch boggle program `python main.py`
